# Dictionaries are mutable datatype
# it is uused to store data values in key - pairs
# they are unordered 
# they do not allow duplicate keys

name = {
    "first" : 90,
    "second" : 90.45,
    "first"  : "hello",
    
}
# cannot have duplicte keys

print(type(name))
print(name)